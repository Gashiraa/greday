$("#expand").modal(
    {
        allowMultiple: true,
        detachable: false
    });

$("#expand").find(".expand").html("<%= j(render 'list') %>");

$('#expand').modal('show');

$(document).trigger('turbolinks:load');
$("#modal-window").modal({
    allowMultiple: true,
    detachable: false
});

$("#modal-window").find(".content").html("<%= j(render 'form') %>");

$('#modal-window').modal('show');

$(document).trigger('turbolinks:load');
